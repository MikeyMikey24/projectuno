import Head from "next/head";
import { Button, HStack, Img, Modal, ModalCloseButton, ModalContent, ModalOverlay, Switch, Text, useDisclosure, VStack } from "@chakra-ui/react";
import Router from "next/router";
import { useState } from "react";

export default function Home() {
    const [show, setShow] = useState(false);
    const { isOpen, onOpen, onClose } = useDisclosure()
    const [dark, setDark] = useState(false);
    let profilePic = '/Mikmik.jpg'
    let logo = 'projectUNO.png'
  return (
    <>
      <Head>
        <title>Mikmik</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <VStack 
      color = {dark ? 'white':'black'}
      bgImg= {dark? "/DarkMode.jpg": "/LightMode.jpg"}
      bgSize="100%"
      width="100%"
      height="100vh"
      >
      <HStack 
      mt='8'
      justifyContent={"space-between"}
      spacing="1100"
      >
         <HStack justifyContent={'flex-start'}>
         <Img  
        onClick={() => Router.push({ pathname: "/" })}  
          h='50'
          w='15vh'
          src = {logo}/>
        <Switch
                  paddingTop={"1"}
                  colorScheme={"teal"}
                  size={"md"}
                  onChange={() => setDark(!dark)}
                />
        </HStack>

        <HStack justifyContent="flex-end" spacing="5">
        <Text 
        _hover={{cursor:'pointer',color:'darkblue', transform:'scale(1.2)', transitionDuration:'.2s'}}
        onClick={() => Router.push({ pathname: "https://www.facebook.com/Mikmikey24/" })}
        as='b'>
        </Text>
        <Img borderRadius='full'
        boxSize=' 35px'
        src = {profilePic}
        onClick={onOpen}
        alt='Mikmik'
        />      
           <Button
          colorScheme="DarkCyan"
          onClick={() => Router.push({ pathname: "/dashboard" })}>
          Dashboard
        </Button>
        <Button
          colorScheme="DarkCyan"
          onClick={() => Router.push({ pathname: "/" })}>
          Log out
        </Button>
        </HStack>
      </HStack>
      </VStack>

      <Modal 
        isOpen={isOpen} onClose={onClose}>
        <ModalOverlay/>
        <ModalContent borderRadius='full'>
        <ModalCloseButton color='white'/>
        <Img
        borderRadius='2%'
          boxSize='100%'
          src = {profilePic}
          alt='Mikmik'
        />      
        </ModalContent>
        </Modal>
    </>
  );
}